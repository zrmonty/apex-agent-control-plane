[package]
name = "apex-security"
version = "0.1.0"
edition = "2024"
publish = false

[dependencies]
apex-domain = { path = "../apex-domain" }
getrandom = "0.4.3"
serde = { version = "1", features = ["derive"] }
sha2 = "0.10.9"

[lints.rust]
unsafe_code = "forbid"
