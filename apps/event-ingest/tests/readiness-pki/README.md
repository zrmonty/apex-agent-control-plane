Synthetic public test keys copied from `apps/mcp-gateway/src/managed/bootstrap/tls-role-fixture.ts`.
Never enroll these credentials or use them outside tests. No production material is included.
The test server uses `gateway.test`; the trusted CA intentionally signs distinct client roles so TLS chain acceptance alone cannot satisfy workload authentication.
